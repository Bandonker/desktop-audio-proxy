{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the local smoke window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
