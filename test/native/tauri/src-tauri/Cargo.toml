[package]
name = "dap-native-smoke"
version = "0.1.0"
description = "Desktop Audio Proxy Tauri runtime smoke fixture"
edition = "2021"
publish = false

[build-dependencies]
tauri-build = { version = "2.5.1", features = [] }

[dependencies]
tauri = { version = "2.11.5", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
