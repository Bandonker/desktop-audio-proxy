CI Failure: Job 62805129092 - ESLint / Prettier fixes

Summary

The CI job failed due to linting and formatting errors (ESLint + Prettier). Many issues are auto-fixable; a small set of unused-variable errors must be addressed manually.

Quick autofix

Run these locally to resolve most problems:

```bash
npm ci
npm run lint -- --fix
npx prettier --write src
```

If your repo uses the scripts, you can run:

```bash
npm run lint -- --fix
npm run format
```

Manual fixes

Address the following unused-variable issues by removing or prefixing variables with an underscore when needed:

- `src/__tests__/client.test.ts` — remove or rename unused `_filePath` variable if not required.
- `src/__tests__/server-impl.test.ts` — prefix unused `req`/`res` with `_req`/_`res` where required by signature but unused.
- `src/debugger.ts` — prefix unused loop variables with `_`.
- `src/types.ts` — prefix unused parameters with `_`.

Commit message suggestion

```
fix: lint/format fixes

- Apply eslint --fix and prettier for formatting
- Address remaining unused variable warnings manually
```

Notes

- After fixing locally, run `npm test -- --runInBand` to verify tests.
- Commit and push to trigger CI.

References

- Job: 62805129092 (ESLint/Prettier failures)
- Related job: 62804932293 (package.json JSON syntax error fixed)
